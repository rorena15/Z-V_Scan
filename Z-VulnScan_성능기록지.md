# Z-VulnScan Professional Edition — 성능 기록지

- **문서 목적:** 성능 측정 결과를 표준화된 양식으로 기록·누적하기 위한 템플릿. 내부 의사결정(경영진/개발팀)이 실측 데이터를 근거로 이루어지도록 한다.
- **범위:** Z-VulnScan Professional Edition 전체 제품.
- **작성 기준일:** 2026-08-05

> **⚠️ 이 문서 사용 원칙**
> 이 문서의 "실측 기록" 표는 **실제로 측정을 수행한 뒤에만 값을 채운다.** 예상치·추정치를 실측처럼 기재하지 않는다. 아직 측정하지 않은 항목은 반드시 "측정 예정"으로 남겨둔다 — 이 원칙은 제품 자체의 "정직한 실패 보고"(§6-2, 접속 실패를 안전으로 위장하지 않음) 설계 철학과 동일한 기준을 문서에도 적용한 것이다. 이번 세션에는 별도 테스트베드가 없어 실측을 진행하지 못했다.

---

## 1. 측정 환경 기록란

실측 시 아래 정보를 함께 기록한다. (PROJECT_MANIFEST.md에 기재된 실제 테스트 VM 3대 활용 권장)

| 항목 | 내용 |
|---|---|
| 측정일자 | |
| 측정자 | |
| Z-VulnScan 버전(`AppConfig.VERSION`) | |
| 테스트 VM 사양 (CPU/RAM/OS) | |
| 네트워크 구성 (대역폭, 대상 호스트 수, 세그먼트 특성) | |
| OT/저속 모드 ON/OFF | |
| 병렬처리량 설정값 | |

## 2. 측정 항목 정의

| 항목 | 측정 방법 | 단위 | 관련 코드 위치 |
|---|---|---|---|
| Discovery 처리 속도 | 호스트 N대 대상 Discovery 시작~완료 소요시간 / N | 초/호스트 | `worker.py` discover_target, `advanced_scanner.py` |
| Audit 처리 속도 (엔진별) | SSH/WinRM/DB 각각 호스트 1대 전체 룰셋 완료 소요시간 | 초/호스트 | `ssh_inspector.py`/`windows_inspector.py`/`database_inspector.py` |
| 동시성별 처리량 | 병렬처리량(1~30) 값을 바꿔가며 N대 완료까지 소요시간 측정 | 호스트/분 | `ThreadPoolExecutor(max_workers=...)` |
| OT 모드 오버헤드 | 동일 대상군을 OT 모드 ON/OFF 각각으로 스캔, 소요시간 차이 | %, 배수 | `AdaptiveThrottle`, `ot_mode` 분기 |
| 메모리 사용량 | 스캔 진행 중 프로세스 RSS 최대값 | MB | (OS 작업관리자/Process Explorer 등 외부 도구 병행 측정) |
| hostname 식별률 | (역DNS 성공 + Audit 실측 성공 + 매핑 성공) / 전체 대상 | % | `advanced_scanner.resolve_hostname`, `worker._extract_real_hostname` |
| 커버리지 완결률 | 판정 완료 자산 / 전체 등록 자산 | % | 로드맵 항목(§4.2, 표준제안서 참고) — 구현 후 측정 |
| 판정 정확도 (오탐/미탐) | 알려진 취약/양호 상태의 테스트 자산 대비 판정 일치율 | % | `utils/rule_judge.py` |
| DB 저장 처리량 | `db_queue` 적재 속도 대비 `db_writer` 소진 속도 | 건/초 | `worker.py` db_writer |

## 3. 실측 기록

| 측정일 | 버전 | 항목 | 환경 요약 | 측정값 | 비고 |
|---|---|---|---|---|---|
| *측정 예정* | | | | | |

*(실측 수행 시 이 표에 행을 추가한다. 회귀 여부를 추적할 수 있도록 버전별로 기록을 누적한다.)*

## 4. 알려진 성능 관련 이슈 (코드 검토 기반, 실측 아님)

이번 세션 코드 검토에서 확인된, 실측이 필요한 성능 관련 사항. 수치가 아니라 **구조적 사실**이므로 실측 대기 없이도 기재 가능하다.

- **포트 스캔의 포트 단위 병렬성 부재** — 호스트 단위(`ThreadPoolExecutor`)는 병렬이나, 한 호스트 내 `tcp_scan()`의 포트 루프 자체는 순차 실행이다. 대용량 커스텀 포트 범위 + 응답 없는 대상 조합에서 스캔 시간이 선형적으로 늘어날 것으로 예상되나, 실제 증가폭은 측정 필요.
- **OT 모드의 처리량 트레이드오프** — 설계상 의도된 동작(동시성 3 + 포트당 0.05초 딜레이 + 적응형 백오프)이므로 "느림"이 아니라 "의도된 안전 여유"다. 다만 일반 모드 대비 얼마나 느려지는지는 실측으로 정량화해 영업 자료(§표준제안서 §3.3)에 구체적 수치로 제시할 가치가 있다.
- **DB 쓰기 스레드 단일화** — `db_writer`가 단일 스레드로 큐를 소진하는 구조라, 대규모 동시 스캔 시 DB 쓰기가 병목이 될 가능성이 이론적으로 있음. 실측 필요.
