# Z-VulnScan Professional Edition — 표준 제안서

- **문서 목적:** 경영진/개발팀 내부 의사결정용. 제품 방향성·차별점·투자 우선순위를 판단하기 위한 근거 자료.
- **범위:** Z-VulnScan Professional Edition 전체 제품 (기 구현 기능 + 로드맵 제안 포함, 상태를 명확히 구분 표기).
- **작성 기준일:** 2026-08-05
- **상태 표기 기준:** ✅ 구현·검증됨 / 🔧 구현됐으나 한계 있음 / 🟡 코드는 있으나 비활성 / 💡 로드맵 제안(미착수)

---

## 1. 배경 및 문제 정의

Z-VulnScan은 **주요정보통신기반시설 기술적 취약점 분석·평가 방법 상세가이드(KISA)** 기준을 따르는 취약점 진단 도구다. 이 시장의 핵심 특성은 다음과 같다.

- 진단 대상이 일반 IT망뿐 아니라 **OT/ICS(산업제어) 망과 맞닿은 L2~L4 구간**을 포함하는 경우가 흔하다.
- OT 인접 구간은 DNS/SNMP 같은 표준 IT 인프라가 없거나 꺼져 있는 경우가 많고, 이는 KISA 하드닝 기준상 "꺼져 있는 게 정상"이기도 하다 — 즉 범용 스캐너가 기대는 전제 자체가 이 시장에서는 자주 깨진다.
- 감사 대상 기관은 "몇 %를 스캔했는가"가 아니라 **"지정된 자산 전부에 판정이 나왔는가"**로 감사 완결성을 평가받는다. 일부 자산이 "점검불가"로 리포트에 구멍이 나면 감사 신뢰도 전체가 흔들린다.

## 2. 제품 개요

3계층 구조(GUI/Engine/Data)로 설계되어 있으며, 핵심 파이프라인은 **Discovery(비인증 자산 탐지) → Audit(인증 정밀 진단) → 판정(6단계) → 리포트(Excel/PDF/TXT)** 순으로 동작한다. 등록 없이도(계정정보 없이) 자산 존재·개방 포트·서비스 배너까지는 탐지되며, 인증 정보가 있으면 SSH/WinRM/DB 연결을 통해 KISA 세부 항목을 실제 판정한다.

라이선스는 Standard/Professional/Enterprise 3단계로, 증적·조치방안 노출 범위와 전문가 모드 사용 가능 여부가 등급에 따라 달라진다.

## 3. 핵심 차별점

### 3.1 KISA 룰셋 커버리지 (기 구현)

| 룰셋 | 대상 | 항목 수 | 라이브 연결 |
|---|---|---:|:---:|
| `linux_rules.json` | Unix/Linux (U-xx) | 67 | ✅ |
| `windows_rules.json` | Windows Server (W-xx) | 64 | ✅ |
| `pc_rules.json` | 업무용 PC (PC-xx) | 18 | ✅ |
| `web_rules.json` | 웹서비스 (WEB-xx) | 26 | ✅ |
| `mysql_rules.json` | MySQL (D-xx) | 17 | ✅ |
| `postgresql_rules.json` | PostgreSQL (D-xx) | 17 | ✅ |
| `mssql_rules.json` | MSSQL (D-xx) | 21 | 🔧 배선 완료, 실서버 검증 전 |
| `oracle_rules.json` | Oracle (D-xx) | 22 | 💡 룰셋만 존재, 커넥션 미배선 |
| **합계 (라이브)** | | **230** | |

경쟁 구도 대비 위치: 퀄리스(Qualys)는 범용 글로벌 스캐너로 KISA 세부기준에 맞춰 나온 도구가 아니고, 클레로티(Claroty)류 OT 전문 플랫폼은 SPAN 포트 기반 수동(passive) 모니터링 방식이라 이 제품처럼 자산에 직접 접속해 KISA 코드 단위로 판정하는 방식과 카테고리가 다르다. Z-VulnScan은 "KISA 세부기준에 정확히 맞춰진 에이전트리스 경량 진단"이라는 위치를 갖는다.

### 3.2 정직한 실패 보고 (기 구현)

접속 실패를 "안전"으로 위장하지 않고 "점검불가"로 명시한다(데모 모드 기본 OFF). 감사 신뢰성의 기본 전제이며, 경쟁 제품이 흔히 갖는 "결과 없음=양호 처리" 함정을 구조적으로 차단한다.

### 3.3 OT/저속 모드 (기 구현)

동시 스캔 대상 수 축소(기본 3), 포트당 딜레이, 응답 지연 감지 시 자동 백오프(AdaptiveThrottle)를 통해 레거시/OT 장비에 부담을 최소화한다. 기본값이 ON으로 설정되어 "안전 우선"이 설계 전제다.

### 3.4 자산/hostname 식별 — 로드맵 (💡 제안)

가장 방어 가능한 차별점으로 판단되는 영역. 상세는 §4 참고.

### 3.5 룰셋 증분 업데이트 (기 구현, 2026-08-05)

KISA 가이드 개정을 exe 재설치 없이 반영할 수 있다. GitHub Release에 첨부된 서명된 룰셋 번들을 받아 Ed25519 서명을 검증한 뒤에만(실패 시 무조건 폐기, 파일 미접촉) exe 옆 `rules/`에 반영한다. 룰의 `command` 필드가 진단 대상 호스트에서 실제 실행되는 구조라(§4-2, RCE 공급망 리스크), 이 서명 검증은 "있으면 좋은 기능"이 아니라 이 제품의 신뢰성을 지키는 필수 안전장치다. 기본 OFF(옵트인) — OT/에어갭 환경을 배려한 설계는 §3.3의 OT/저속 모드와 같은 철학을 공유한다. exe 자체의 자동 업데이트는 의도적으로 범위 밖(코드사이닝 미비 상태에서 자동 자기교체는 오히려 백신 오탐·공급망 공격처럼 보일 위험이 큼).

### 3.6 UI/UX 리디자인 (기 구현, 2026-08-05)

국산 보안 콘솔(VADA/엑소스피어/솔리드스텝)·Qualys 계열이 공유하는 "전문적이며 친화적" 문법으로 전면 재설계. 탭 역할도 명확히 분리해, 대시보드는 읽기 전용 현황판으로만 남기고 조치가 필요한 진단 결과(Waiver/Expert/수동입력)는 자산 탭으로 옮겼다 — §5의 "확실성" 포지셔닝(대기업 대상)과 결이 맞는 변화다: 화면 구성 자체가 "무엇이 되어 있고 무엇이 안 되어 있는지"를 흐리지 않고 정직하게 보여준다.

## 4. 로드맵 제안 — 자산/hostname 식별 4단 폴백 + 커버리지 추적

### 4.1 배경

DNS/SNMP가 없는 OT 구간에서는 범용 스캐너가 IP만 남기고 자산 식별에 실패한다. 이를 다단 폴백 구조로 해결하는 것을 제안한다.

| 단계 | 방법 | 상태 | 비고 |
|---|---|---|---|
| 1 | 역DNS(PTR) | ✅ 기 구현 | 1초 타임아웃, DNS 없는 구간에서 무력화됨 |
| 2 | Audit 성공 시 실측 hostname (`uname`/`systeminfo`) | ✅ 기 구현 | 인증 접속 성공이 전제 |
| 3 | 자산 목록(엑셀/CSV) 매핑 + 실제 스캔 결과와 diff(누락·유령 자산 탐지) | ✅ 구현 완료 (2026-08-05) | hostname 컬럼 인식 → 역DNS 실패 시 폴백으로 사용. 스캔 종료 시 "선언 vs 실제 발견" diff를 로그+완료 팝업에 표기 |
| 4 | 수동 진단 입력(육안 점검) | ✅ 기 구현, UX 개선 우선순위 상향 | 어떤 통제 수준에서도 막히지 않는 최종 보루 |
| (보류) | SNMP sysName/NetBIOS | 💡 제안, 기본 OFF 권장 | KISA 하드닝 기준상 "꺼져 있는 게 양호"인 서비스라 하드닝된 자산엔 안 통함 — 옵트인으로만 |
| (보류) | 로컬 스크립트 킷(OT 에이전트, `pc_toolkit.py` 확장) | 💡 제안, 재검토 필요 | OT망은 파일 반입 자체가 통제되는 경우가 많아 "파일 반입은 가능하나 SSH/WinRM만 막힌" 좁은 케이스에만 유효. 현장 비중 확인 후 투자 여부 재검토 |

### 4.2 커버리지 추적 시스템 (✅ 구현 완료, 2026-08-05)

발견됐지만(TBL_ASSETS) 최신 회차에 실제 KISA 판정 코드가 하나도 없는 자산을 "미해결"로 판별해(`get_unresolved_assets()`, Discovery 메타 코드 SYS-/TCP-/UDP-/INFO-/CONN-만 있는 경우도 미해결로 취급), 대시보드에 경고 배너로 표시하고 Excel "1.종합요약" 시트에 "■ 커버리지(감사 완결성)" 섹션으로 명시한다(0건이어도 "미해결 자산 없음"으로 정직하게 표기, 침묵하지 않음). 목적은 두 가지다.

1. **완결성 보장** — 감사 종료 후 "이 자산은 아무도 안 봤다"는 상황을 구조적으로 방지.
2. **감사 대응 근거자료** — 미해결 항목과 그 사유를 리포트에 명시해, 이 진단 결과 자체가 상위 감사에 대응할 수 있는 근거 문서가 되게 함.

## 5. 타겟 세그먼트별 포지셔닝

| 세그먼트 | 구매 논리 | 포지셔닝 |
|---|---|---|
| 대기업 (현재 영업 주력 대상) | "싸다"보다 "확실하다"에 예산 편성 | 가격 경쟁이 아니라 "퀄리스/클레로티도 못 하는, 이 규제·이 망 구조에 정확히 맞는 진단"이라는 확실성 프레임 |
| 중소/영세 사업자 | 퀄리스/클레로티 라이선스 비용 자체가 접근 불가 | 동일 기능을 감당 가능한 가격에 제공 — 경쟁 자체가 없는 시장 |

## 6. 알려진 리스크 (정직한 공개)

| 항목 | 내용 | 우선순위 |
|---|---|---|
| DB 룰셋의 "쿼리 성공+뷰 제약으로 조용히 0건" 잔여 리스크 | `execute_query()` 예외 시엔 이미 MANUAL 처리되어 안전하나, 예외 없이 빈 결과가 나오는 극히 좁은 케이스는 실 DB 테스트베드 없이는 검증 불가 | 낮음 (확신도 낮음, 재현 사례 없음) |
| 포트 스캔 포트 단위 병렬성 부재 | 호스트 단위는 병렬이나 호스트 내 포트 루프는 순차 | 중간 |
| Oracle 룰셋 미배선 | JSON만 존재, 라이브 연결 안 됨 | 낮음~중간 (고객 수요에 따라) |
| 죽은 코드 추정 (`network_visualizer.py` 등) | 정리 대상, 기능에 영향 없음 | 낮음 |

## 7. 결론

Z-VulnScan은 이미 KISA 5개 룰셋(230개 라이브 항목)을 갖춘 실사용 가능한 제품이며, OT 특화 hostname/커버리지 로드맵을 실행하면 "범용 스캐너 대비 압도적 차별점 + OT 전문 플랫폼 대비 경량 대안"이라는 이중 포지셔닝이 가능하다. 우선순위는 §4의 로드맵 표 순서를 권장한다.
