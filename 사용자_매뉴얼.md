# Z-VulnScan Professional Edition 사용자 매뉴얼

이 문서는 Z-VulnScan Professional Edition을 실제로 사용하는 담당자를 위한 매뉴얼입니다.

> 이 문서 안의 버튼/메뉴 이름은 실제 화면에 표시되는 문구를 그대로 옮긴 것입니다.
> 화면 구성은 버전에 따라 조금씩 달라질 수 있습니다. (기준 버전: Z-Vuln Scan v3.3.0)

---

## 목차

1. [제품 소개](#1-제품-소개)
2. [최초 실행](#2-최초-실행)
3. [라이선스 활성화](#3-라이선스-활성화)
4. [화면 구성](#4-화면-구성)
5. [대시보드](#5-대시보드)
6. [스캔 (Discovery / Audit)](#6-스캔-discovery--audit)
7. [자산 관리](#7-자산-관리)
8. [전문가 모드 (Enterprise)](#8-전문가-모드-enterprise)
9. [예외처리(Waiver) 관리 (Enterprise)](#9-예외처리waiver-관리-enterprise)
10. [수동 진단 입력](#10-수동-진단-입력)
11. [교차검증 (Cross-check)](#11-교차검증-cross-check)
12. [PC 진단 도구](#12-pc-진단-도구)
13. [리포트 생성](#13-리포트-생성)
14. [설정](#14-설정)
15. [문제 해결 / FAQ](#15-문제-해결--faq)
16. [부록](#16-부록)

---

## 1. 제품 소개

Z-VulnScan Professional Edition은 **네트워크 자산을 탐지하고, KISA(한국인터넷진흥원)
2026년 개정 기준에 따라 원격으로 접속해 기술적 취약점을 점검**하는 도구입니다.

**이 도구가 아닌 것**: 모의해킹(침투테스트) 도구가 아닙니다. 취약점을 공격하거나
악용하는 기능은 없으며, 공식 점검 가이드에 정의된 항목을 순서대로 확인하고
결과를 기록하는 용도입니다.

**사용 원칙**
- 반드시 **점검 권한이 있는 자산**에만 사용해야 합니다. 권한 없는 대상에 대한 스캔은
  정보통신망법 등 관련 법령에 따라 형사·민사 책임을 질 수 있습니다.
- 권장 용도: 내부 자산 인벤토리 관리, 정기 취약점 점검, 감사 대비, 보안 교육
- 금지 용도: 무단 침투테스트, 무작위 외부 스캔, 서비스 거부(DoS) 실험, 제3자 자산에
  대한 비인가 접근

---

## 2. 최초 실행

### 2.1 법적 고지 동의

프로그램을 처음 실행하면 메인 화면보다 먼저 **법적 고지(Legal Disclaimer) 창**이 뜹니다.

- 고지 내용을 확인한 뒤 **"위 법적 고지 내용을 모두 읽었으며, 이에 동의합니다"**
  체크박스를 선택해야 **"I Agree & Start"** 버튼이 활성화됩니다.
- 동의하지 않고 **"Decline (Exit)"**을 누르면 프로그램이 즉시 종료됩니다.
- 동의 시점(시각), 실행 계정, PC 이름이 `audit_agreement.log`에 자동 기록됩니다.
  (감사 대응용 근거 기록이므로 별도 조작 불필요)

### 2.2 DB 암호화 키 생성 (최초 1회)

최초 실행 시 스캔 결과를 저장할 로컬 DB를 위한 암호화 키가 자동 생성됩니다.
안내 창이 뜨면 내용을 확인하고 넘어가면 됩니다. 이후 프로그램을 정상 종료할 때마다
DB 파일이 자동으로 암호화되고, 다음 실행 시 자동으로 복호화됩니다. 별도 조작은
필요 없습니다.

### 2.3 창 제목으로 현재 상태 확인

메인 화면 상단 창 제목은 `Z-Vuln Scan v3.0 Standard` 처럼 **현재 라이선스 등급**을
함께 보여줍니다. 상단바 우측에도 `{담당자명} · {등급}` 형태로 항상 표시되므로,
지금 어떤 등급으로 실행 중인지는 화면 어디서나 바로 확인할 수 있습니다.

---

## 3. 라이선스 활성화

### 3.1 등급 종류

라이선스 키를 등록하지 않으면 **Standard** 등급으로 시작합니다.
| 항목 | Standard | Professional | Enterprise |
|---|:---:|:---:|:---:|
| PDF 리포트 | O | O | O |
| Excel 리포트 | - | O | O |
| TXT 리포트 | O | O | O |
| 상세 증적(Evidence) | 취약·부분만족 항목만 | 전체 항목 | 전체 항목 |
| 조치 방안 | 미제공 | 위험도 상/중 항목만 | 전체 항목 |
| 전문가 모드 | - | - | O |
| 예외처리(Waiver) 관리 | - | - | O |

> **용어**: "상세 증적"은 점검 중 실제로 수집된 원본 명령 실행 결과입니다.
> "조치 방안"은 취약점을 어떻게 고쳐야 하는지 안내하는 텍스트입니다.

### 3.2 라이선스 키 등록하는 법

1. 사이드바 하단 **설정** → 좌측 탭에서 **라이선스** 선택
2. **"라이선스 키 입력"** 버튼 클릭
3. 뜨는 창(제목: *Product Activation*)에 라이선스 키를 붙여넣습니다.
   형식은 `XXX-XX-XXXX-XXXX-XXXX` (5개 구간, 하이픈으로 구분)입니다.
   입력창 옆 **"표시"** 버튼으로 입력한 키를 눈으로 확인할 수 있습니다.
4. **"Activate License"** 클릭
   - 성공: *"정품 인증이 완료되었습니다."* 와 함께 등급/만료일이 표시됩니다.
   - 실패: *"유효하지 않거나 만료된 라이선스 키입니다."* — 키를 다시 확인하세요.
5. 인증이 끝나면 창을 닫는 즉시 화면이 새 등급에 맞게 바로 갱신됩니다
   (프로그램 재시작 불필요).

키는 **한 번에 하나만** 등록됩니다. 새 키를 등록하면 기존 키를 완전히 대체합니다.

### 3.3 라이선스 삭제

설정 → 라이선스 탭 → **"라이선스 삭제"** 버튼 (등록된 키가 있을 때만 활성화됩니다).
확인 창에서 한 번 더 확인 후 삭제되며, 이후 Standard 등급으로 돌아갑니다.

### 3.4 지금 등급에서 쓸 수 있는 기능 확인하기

설정 → 라이선스 탭 하단에 현재 등급 기준으로 아래 4가지가 실시간으로 표시됩니다.
등급을 바꿀 때마다 이 문구가 즉시 갱신되므로, 헷갈릴 땐 이 화면을 확인하면 됩니다.

- Excel 리포트: 가능 / 불가 (PDF만 가능)
- 상세 증적(Evidence): 전체 항목 제공 / 취약·부분만족 항목만 제공
- 조치 방안: 전체 제공 / 상·중 항목만 제공 / 미제공
- 전문가 모드: 사용 가능 / 사용 불가 (Enterprise 전용)

---

## 4. 화면 구성

좌측에 세로 사이드바가 있고, 클릭하면 오른쪽 화면이 바뀌는 방식입니다.

| 메뉴 | 설명 |
|---|---|
| **대시보드** | 현재 상황 요약 (기본 화면) |
| **스캔** | 자산 탐지 / 취약점 진단 실행 |
| **자산** | 발견된 자산 목록과 진단 결과 관리 |
| **리포트** | PDF/Excel/TXT 결과 리포트 생성 |
| **교차검증** | 외부에서 가져온 텍스트 결과와 대조 (별도 창으로 열림) |
| **PC 진단** | WinRM이 안 되는 PC용 로컬 진단 스크립트 생성/결과 반영 (별도 창으로 열림) |
| **도움말** | 화면별 설명 (하단, 별도 창) |
| **설정** | 각종 환경설정 (하단, 별도 창) |

상단바에는 현재 위치(어느 메뉴에 있는지)와 `{담당자} · {라이선스 등급}` 정보가
항상 표시됩니다.

---

## 5. 대시보드

메인 화면에 들어오면 가장 먼저 보이는 화면으로, 4개의 요약 카드와 최근 활동
목록으로 구성됩니다.

**요약 카드 4개**
1. **스캔된 자산** — 지금까지 발견된 자산 총 개수
2. **취약 항목** — 취약(Vulnerable) 판정 건수, 직전 회차 대비 증감(▲/▼) 표시
3. **부분만족** — 부분만족(Partial) 판정 건수, 마찬가지로 증감 표시
4. **보안수준** — 도넛 그래프로 표시되는 전체 보안 점수(%). 스캔 이력이 없으면
   숫자 대신 "-"로 표시되며, 임의로 점수를 만들어 보여주지 않습니다. 이 계산식은
   Excel 리포트 표지의 보안수준 숫자와 동일한 산식을 사용합니다.

**최근 활동**
과거 스캔 세션 목록입니다. 기간(최근 1일/7일/30일/전체)을 필터링할 수 있고,
목록의 특정 항목을 클릭하면 **자산** 화면으로 이동하면서 그 세션에 해당하는
결과만 자동으로 필터링되어 보입니다 (다시 전체를 보려면 필터 해제 버튼 사용).

**미해결 자산 경고**
자산은 발견됐지만 아직 어떤 항목으로도 진단(Audit)이 이루어지지 않은 자산이 있으면
대시보드 상단에 경고 배너가 자동으로 나타납니다. 점검 누락을 놓치지 않기 위한
장치입니다.

---

## 6. 스캔 (Discovery / Audit)

**스캔** 화면에는 두 가지 실행 방식이 있습니다. 버튼도 별도로 나뉘어 있으니 목적에
맞는 버튼을 눌러야 합니다.

### 6.1 Discovery Only (자산 탐지만)

- 계정 정보 없이 **핑/포트 스캔/배너 확인**만 수행해 자산 목록을 만듭니다.
- **KISA 딥 진단은 절대 수행하지 않습니다.** 계정 정보를 입력해뒀더라도 마찬가지입니다.
- IP 범위/CIDR 등 여러 대상을 한 번에 입력할 수 있습니다.

### 6.2 Start Audit (KISA 취약점 진단)

- 계정으로 실제 접속해 KISA 룰셋 기준 상세 점검을 수행합니다.
- **단일 IP만 지원**합니다. `/`가 포함된 대역(CIDR)을 넣으면 즉시
  *"Audit은 단일 IP만 지원합니다"* 경고가 뜹니다.
- 접속 계정(User/Password)이 필요합니다. 단, 대상이 데모용 IP(`127.0.0.1`,
  `localhost`, `0.0.0.0`)인 경우에는 계정 없이도 실행됩니다.
- 이미 Discovery로 탐지한 적 있는 IP라면 그때 확인한 OS/포트 정보를 재사용해
  다시 포트 스캔을 하지 않습니다. 처음 보는 IP라면 Audit 시작 전에 Discovery를
  자동으로 먼저 수행합니다.
- **접속 실패는 항상 "접속 실패로 점검 불가"로 정직하게 기록됩니다.** 임의로 양호
  판정을 내리지 않습니다. (단, 아래 "데모 모드"가 켜져 있을 때는 예외입니다 — 실제
  현장 점검에서는 반드시 꺼두세요.)

### 6.3 입력 항목 전체

| 항목 | 설명 |
|---|---|
| **대상 범위** | 단일 IP(`192.168.1.1`), 범위(`10.0.0.1-50`), CIDR(`192.168.1.0/24`) 모두 입력 가능. 자산 화면의 "가져오기"로 채워진 목록에서 선택해도 됩니다. |
| **스캔 모드** | `Fast scan`(주요 포트만, 빠름) / `Custom`(직접 지정 또는 "선택..." 버튼으로 포트 선택) / `Full scan (1-65535)`(전체 포트, 시간이 오래 걸려 확인 창이 한 번 더 뜹니다) |
| **User / Password** | SSH·WinRM 접속 계정 |
| **DB 계정이 SSH/WinRM 계정과 다름** | 체크 시 DB 전용 계정 입력란이 별도로 나타남 (DBA 계정이 OS 관리 계정과 다를 때) |
| **대상에 Oracle DB 포함** | 체크 시 Oracle 서비스명 입력란이 나타남 (비워두면 기본값 `ORCL`로 시도) |
| **동시 처리 대수 (1~30)** | 슬라이더. 숫자가 낮을수록 네트워크에 부담이 적고 느립니다. OT망은 1~3 권장 |
| **OT 안전 모드** | 기본값 켜짐. 산업용/레거시/임베디드 장비 대상 시 동시 처리 수를 자동으로 낮게 제한하고, 응답이 느려지면 스스로 속도를 더 늦춥니다 |
| **데모 모드** | **기본값 꺼짐. 실제 현장 점검에서는 절대 켜지 마세요.** 켜면 `127.0.0.1`/`localhost`/`0.0.0.0` 등 시연용 대상에 가짜 결과를 채워 넣고, 실제 접속 실패도 가짜 데이터로 대체합니다 |
| **담당자** | 점검을 수행한 담당자 이름. 결과에 함께 기록되어 이력 추적에 사용되며, 상단바 표시에도 반영됩니다 |

> **주의**: 자산 화면에서 `127.0.0.1`/`0.0.0.0` 같은 데모용 자산을 더블클릭하면
> 시연용 계정(root/toor 등)이 자동으로 채워지는 편의 기능이 있습니다. 이는 위의
> "데모 모드" 체크박스와는 **별개**이니 혼동하지 마세요.

### 6.4 지원 진단 대상

Linux, Windows, PC(윈도우 로컬 진단 포함), Web/WAS, MySQL, PostgreSQL, MSSQL,
Oracle을 지원합니다. (KISA 2026년 개정 기준)

---

## 7. 자산 관리

**자산** 화면은 두 개의 표로 구성됩니다.

**상단: 자산 목록** — IP / 호스트명 / 출처 / OS 종류 / MAC 주소 / 설명 / 제조사

- **DB Manager**: 자산 정보를 직접 편집하는 창을 엽니다 (7.1 참고)
- **자산평가**: C/I/A 기반 자산 등급을 매기는 창을 엽니다 (7.2 참고)
- **가져오기**: 고객사가 제공한 CSV/Excel 자산 목록을 불러옵니다. "IP"가 포함된
  헤더를 자동으로 찾아 인식하며, 없으면 셀 내용에서 IP 형태를 자동 탐색합니다.
  이렇게 가져온 목록에 없는 IP는 스캔 대상 후보에서 자동 제외되어, 실제 점검 범위가
  고객사와 합의한 범위와 정확히 일치하도록 돕습니다.
- **내보내기**: 자산 목록을 파일로 저장
- **Clear List**: 목록 초기화. 아직 리포트로 출력하지 않은 결과가 있으면 먼저 경고가
  뜹니다.
- 자산 행 위에서 **마우스 우클릭**: 핑 확인, RDP 접속(Windows)/SSH 접속(Linux),
  IP 복사

**하단: 진단 결과** — Host / OS / KISA 코드 / 판정 상태 / 위험도

- 상태는 색으로 구분됩니다: 취약(빨강) / 부분만족(노랑) / 양호(초록) / 수동확인 필요(회색)
- **Waiver**, **Expert** 버튼은 **Enterprise 등급에서만 보입니다** (8·9장 참고)
- **수동 진단 입력**: 원격 접속이 불가능한 자산의 결과를 사람이 직접 입력 (10장 참고)
- **Refresh**: 결과 다시 불러오기

### 7.1 DB Manager

"자산 목록" 버튼 옆의 **DB Manager**를 열면 등록된 모든 자산을 표 형태로 직접
수정할 수 있습니다 (더블클릭으로 편집). IP/ID/Port/최종 확인일은 수정할 수 없습니다.
구역 태그(Zone Tag)로 필터링할 수 있고, 선택한 자산을 삭제하면 **관련 스캔 기록도
함께 영구 삭제**되므로 삭제 전 확인 창을 꼭 읽어보세요.

### 7.2 자산평가 (Asset Assessment)

자산별로 용도/부서/담당자와 함께 **기밀성(C)/무결성(I)/가용성(A)** 점수(각 1~3)를
직접 입력하면, 합산 점수에 따라 등급(상/중/하)이 자동 계산됩니다. 이 값은 Excel
리포트의 "점검대상" 시트와 자산별 상세 시트에 반영됩니다. 재스캔을 하더라도 이
값은 자동으로 덮어써지지 않는, 사람이 입력하는 정보입니다.

---

## 8. 전문가 모드 (Enterprise)

**Enterprise 등급 전용** 기능입니다. 다른 등급에서 열려고 하면
*"전문가 모드는 Enterprise 등급 전용 기능입니다"* 안내와 함께 열리지 않습니다.

자산 화면의 **Expert** 버튼(또는 설정 → 룰셋/전문가 프로필 → "전문가 모드 열기")으로
엽니다.

- 왼쪽에 룰셋 → 카테고리 → 개별 항목 순으로 트리 구조의 체크박스 목록이 있습니다.
  각 단계마다 "포함된 개수/전체 개수"가 실시간으로 표시됩니다.
- **체크 해제한 항목은 다음 진단에서 아예 실행되지 않습니다** ("해당없음"조차 기록되지
  않고, 결과 자체가 생성되지 않습니다). 예: 웹 서비스가 없는 서버라면 WEB 관련 항목을
  빼고 진단할 수 있습니다.
- 오른쪽에는 선택한 항목의 상세 정보(항목명/중요도/카테고리/실행되는 명령어)가
  표시되지만, 이 내용은 **읽기 전용**이며 편집할 수 없습니다 — 포함/제외 여부만
  바꿀 수 있습니다.
- 하단에 항상 "현재 몇 개 항목이 제외되어 있는지" 경고 문구가 표시됩니다.

> **9장의 예외처리(Waiver)와 다른 점**: 전문가 모드는 **스캔 전에** 점검 범위 자체를
> 정하는 기능이고, Waiver는 **스캔이 끝난 뒤** 이미 나온 취약 결과를 예외로 인정하는
> 기능입니다.

---

## 9. 예외처리(Waiver) 관리 (Enterprise)

**Enterprise 등급 전용**입니다. 자산 화면의 **Waiver** 버튼(또는 설정 → 룰셋/전문가
프로필 → "예외처리(Waiver) 관리 열기")으로 엽니다.

"취약" 판정을 받은 항목이라도 (예: 곧 폐기 예정 서버, 다른 방식으로 이미 위험이
완화된 경우) 결과를 숨기지 않으면서 공식적으로 예외 처리할 수 있습니다.

- 자산을 선택한 뒤 결과 목록에서 항목을 선택하고 **"선택 항목 예외처리"** 클릭
- 반드시 **사유**와 **승인자**를 입력해야 합니다 (둘 다 비워둘 수 없음)
- 예외처리된 항목은 리포트에 "예외처리됨"으로 계속 표시되지만, 점수 계산에서는
  감점 대상에서 제외됩니다
- 필요하면 **"선택 항목 예외 해제"**로 언제든 되돌릴 수 있습니다

---

## 10. 수동 진단 입력

원격 접속이 안 되는 자산(SSH/WinRM 권한이 없거나, 망분리된 환경 등)의 결과를
**사람이 직접 눈으로 확인한 내용**으로 입력하는 기능입니다. 등급과 무관하게 모든
사용자가 사용할 수 있습니다.

자산 화면의 **"수동 진단 입력"** 버튼으로 엽니다.

- 대상 룰셋(Linux/Windows/PC/Web-WAS/MySQL/PostgreSQL)을 고른 뒤, 각 항목별로
  판정 결과(양호/취약/부분만족/해당없음/수동확인 필요)와 현황, 증적(선택, 비우면
  현황 텍스트로 대체)을 입력합니다.
- 입력 내용은 **입력하는 즉시 자동으로 임시 저장**되며, 창을 다시 열면 이어서 작업할
  수 있습니다. (예전에 자산을 바꾸면 표 전체가 사라지는 문제가 있었는데, 이를 막기
  위해 만들어진 기능입니다.)
- 저장된 결과는 자동 스캔 결과와 동일한 곳에 저장되어, Excel/PDF/TXT 리포트에
  똑같이 반영됩니다. (담당자란에는 "수동입력"으로 자동 표시)

---

## 11. 교차검증 (Cross-check)

이미 가지고 있는 **텍스트 형식의 점검 결과 파일**을 Z-VulnScan의 판정과 서로
대조해보는 독립 기능입니다. 예를 들어 다른 방식으로 먼저 수행해둔 점검 결과가
있을 때, Z-VulnScan의 판정과 얼마나 일치하는지 교차 확인하는 용도로 씁니다.
**등급과 무관하게 항상 사용 가능**합니다.

사이드바의 **교차검증** 버튼으로 엽니다.

두 가지 비교 방식 중 선택합니다.

- **재판정(오프라인, DB 미사용)**: 가져온 텍스트 파일에 담긴 증적 내용을
  Z-VulnScan의 판정 규칙으로 다시 판정해봅니다. 원본 파일의 증적 문구 형식이
  Z-VulnScan과 다를 수 있어 정확도가 낮을 수 있습니다.
- **DB 직접대조 (추천)**: Z-VulnScan이 그 대상을 이미 스캔한 적이 있다면, 그때
  저장된 실제 결과와 직접 비교합니다. 더 정확하며 권장하는 방식입니다.

결과는 일치/불일치/판별불가 등으로 색 구분되어 표시되며, **"엑셀로 내보내기"**로
별도의 교차검증 리포트를 만들 수 있습니다.

> **주의**: 이 기능에 가져오는 텍스트 파일이 본인 또는 소속 조직이 작성/사용 권한을
> 가진 자료인지 확인 후 사용하세요. 제3자가 작성한 자료를 권한 없이 가져와 사용하면
> 별도의 법적 문제가 발생할 수 있습니다.

---

## 12. PC 진단 도구

업무용 PC는 보안 정책상 WinRM 원격 접속이 막혀 있는 경우가 많아, 일반 **스캔**
화면으로는 점검할 수 없습니다. 이럴 때 **PC 진단** 도구를 사용합니다. 사이드바의
**PC 진단** 버튼으로 엽니다.

동작 방식은 2단계입니다.

1. **① 진단 스크립트 생성 및 저장** — 버튼을 누르면 로컬 실행용 진단 스크립트
   파일(.bat) 하나가 만들어집니다. 이 파일을 대상 PC로 옮겨 더블클릭하면
   (관리자 권한 승인 창이 자동으로 뜹니다) 진단이 실행되고, 같은 폴더에
   `[PC]호스트명_OS_IP.txt` 형태의 결과 파일이 생성됩니다. 판정 없이 원본 명령
   출력만 수집하므로 실행 자체는 안전합니다.
2. **② 결과 TXT 선택** → **가져오기 및 DB 반영** — 대상 PC에서 만들어진 결과
   파일을 다시 이 PC로 가져와 불러오면, `Audit`으로 직접 스캔한 것과 동일한
   판정 로직으로 채점되어 정식 스캔 이력에 반영됩니다. 이후 대시보드/리포트에
   그대로 나타납니다.

**출력 설정**: 스크립트 저장 시 기본으로 제안되는 **출력 경로**와 **스크립트
파일명**을 지정할 수 있습니다. 한 번 설정하면 다음에 열 때도 유지됩니다. 다만
스크립트가 만드는 **결과 TXT 파일의 이름 자체는 바꿀 수 없습니다** —
`[PC]호스트명_OS_IP.txt` 구조에서 교차검증 파서가 호스트명/IP를 읽어내기 때문에,
이 이름 규칙이 흐트러지면 결과를 다시 불러올 수 없게 됩니다.

> **참고**: 예전 버전에서는 스크립트 파일이 .ps1과 .bat 두 개로 나뉘어 있어
> 대상 PC로 옮길 때 하나를 빠뜨리는 실수가 잦았습니다. 지금은 파일 하나만
> 옮기면 됩니다.

---

## 13. 리포트 생성

**리포트** 화면 상단의 "리포트 커스터마이징" 항목(회사명/브랜딩 문구, 문서 제목,
저장 파일명)은 **PDF/Excel에만 적용**됩니다. 값을 비워두면 기본값이 사용됩니다.

> TXT 리포트는 고객사의 기존 매크로(엑셀 자동화 도구)와 호환되도록 고정된 형식을
> 쓰기 때문에, 위 커스터마이징 항목이 적용되지 않습니다.

| 형식 | 대상 | 특징 | 접근 가능 등급 |
|---|---|---|---|
| **PDF** | 경영진 보고용 | 표지 요약 + 자산별 요약. 항목별 상세 증적은 포함하지 않음 | 전체 등급 |
| **Excel** | 실무자용 | 항목별 상세 표, 통계, 회차 비교(개선율) 시트까지 포함 | Professional, Enterprise |
| **TXT** | 원본 근거 자료 | 자산별 파일 1개, 실행한 명령어와 원본 출력 그대로 | 전체 등급 |

- **Excel 리포트는 Standard 등급에서 생성 버튼을 눌러도 만들어지지 않습니다**
  ("이 기능은 Professional 라이선스 전용입니다" 안내가 뜹니다). PDF/TXT는 등급과
  무관하게 항상 생성되지만, 등급에 따라 **증적/조치방안의 상세도**가 달라집니다
  (3.1절 표 참고).
- **회차(재점검 이력)**: 같은 서버를 다시 스캔해도 과거 기록은 삭제되지 않고 이력으로
  쌓입니다. 다만 리포트는 **항상 가장 최근 회차만** 반영합니다. 아직 리포트로 뽑지
  않은 결과가 있는 상태에서 자산 목록을 지우려 하면 경고가 뜹니다.

---

## 14. 설정

사이드바 하단 **설정**을 열면 좌측에 7개 탭이 있습니다.

| 탭 | 내용 |
|---|---|
| **로그/이력 보관** | 스캔 이력 보관 기간(1~3650일, 기본 90일) 설정, "지금 오래된 이력 정리 실행" 버튼 |
| **테마·UI** | 다크/라이트 테마 선택(재시작 후 적용), 하단 로그 패널 표시 여부 |
| **리포트 출력** | 리포트 저장 폴더 지정 (비워두면 프로그램 폴더의 `reports`) |
| **룰셋/전문가 프로필** | 전문가 모드·Waiver 관리 바로가기, 현재 룰셋 버전 확인, 룰셋 자동 업데이트 설정(**망분리 환경에서는 꺼두는 것을 권장**하며 기본값도 꺼짐) |
| **기본 계정/자격증명** | 스캔 화면 User란에 자동으로 채울 기본 계정명 설정, 저장된 접속 정보 개별 삭제 |
| **호스트 키(known_hosts)** | 등록된 SSH 서버 지문 목록 관리 (14.1절 참고) |
| **라이선스** | 3장 참고 |

---

## 15. 문제 해결 / FAQ

### 15.1 "이전과 다른 호스트 키" 경고가 뜨며 SSH 접속이 안 됩니다

Z-VulnScan은 같은 IP에 대해 SSH 서버의 지문(호스트 키)을 최초 접속 시 기억해두고,
이후 접속마다 지문이 바뀌지 않았는지 확인합니다(중간자 공격 방지). 서버를 재설치하는
등 **정상적인 이유**로 지문이 바뀌었다면, 설정 → 호스트 키(known_hosts) 탭에서 해당
IP 항목을 삭제한 뒤 다시 접속하세요.

### 15.2 대시보드에 "미해결 자산" 경고가 뜹니다

Discovery로 발견만 되고 아직 어떤 항목으로도 진단되지 않은 자산이 있다는 뜻입니다.
점검 범위 누락을 방지하기 위한 경고이니, 해당 자산에 대해 Audit을 수행하거나
점검 대상이 아니라면 자산 목록에서 정리하세요.

### 15.3 데모 모드를 실수로 켜둔 채 실제 점검을 하면?

접속 실패가 실패로 기록되지 않고 가짜 데이터로 채워질 수 있습니다. **실제 현장
점검 전에는 스캔 화면의 "데모 모드" 체크박스가 꺼져 있는지 항상 확인하세요.**
(기본값은 항상 꺼짐입니다.)

### 15.4 Excel 리포트 생성 버튼이 안 눌립니다 / 막힙니다

현재 라이선스 등급이 Standard입니다. 설정 → 라이선스에서 현재 등급을 확인하고,
Professional 이상 라이선스를 등록하세요 (3.2절 참고).

### 15.5 Waiver/Expert 버튼이 화면에 아예 안 보입니다

Enterprise 등급 전용 기능이라 그 외 등급에서는 버튼 자체가 숨겨집니다. 등급을
확인하려면 설정 → 라이선스를 확인하세요.

---

## 16. 부록

### 16.1 판정 결과 용어

| 표시 | 의미 |
|---|---|
| 양호 (Safe) | 점검 기준을 충족함 |
| 취약 (Vulnerable) | 점검 기준을 충족하지 못함 — 조치 필요 |
| 부분만족 (Partial) | 일부만 충족 |
| 해당없음 (N/A) | 해당 서버/환경에 적용되지 않는 항목 |
| 수동확인 필요 (Manual) | 자동으로 판정할 수 없어 사람의 확인이 필요 |
| 접속 실패로 점검 불가 | 원격 접속 자체가 실패해 점검을 수행하지 못함 |

### 16.2 KISA 코드 접두사

- **U** — Unix/Linux 계열
- **W** — Windows 계열
- **PC** — PC(개인용 컴퓨터) 로컬 진단
- **WEB** — 웹서비스/WAS

### 16.3 지원 진단 대상 (KISA 2026년 개정 기준)

Linux, Windows, PC, Web/WAS, MySQL, PostgreSQL, MSSQL, Oracle

---

*이 매뉴얼은 Z-VulnScan Professional Edition의 실제 화면 동작을 기준으로 작성되었습니다.
화면 문구나 기능은 버전 업데이트에 따라 달라질 수 있습니다.*
