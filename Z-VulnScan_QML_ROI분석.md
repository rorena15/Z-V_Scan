# Z-VulnScan Professional Edition — QML(Qt Quick) 도입 ROI 분석

- **문서 목적:** "대시보드를 더 인터렉티브하게, 애니메이션을 넣고 싶다"는 요구에 대해, 현재 PySide6/QWidgets 기반을 QML(Qt Quick)로 부분/전면 전환하는 게 상품으로서 투자 대비 효과가 있는지 판단하는 근거 자료. 내부 의사결정용(경영진/개발팀).
- **결론 먼저 요약:** **지금 시점에 전면 QML 전환은 ROI가 나오지 않는다고 판단합니다.** 부분 도입도 신중해야 하며, 당장은 QWidgets 안에서 `QPropertyAnimation` 등으로 인터렉티브 효과를 넣는 쪽을 권합니다. 근거는 아래 전체에 상세히 기술합니다.
- **작성 기준일:** 2026-08-05

---

## 1. 검토 대상 정의

"프레임워크를 바꾼다"는 표현이 나올 수 있는 선택지가 여러 개라 먼저 범위를 명확히 한다.

| 선택지 | 내용 |
|---|---|
| A. 현행 유지 + 애니메이션 확장 | PySide6/QWidgets 그대로, `QPropertyAnimation`/`QGraphicsOpacityEffect` 등 Qt 내장 애니메이션 API로 인터렉티브 효과만 추가 |
| B. 부분 QML 도입 | 대시보드 등 특정 화면만 `QQuickWidget`으로 QML 렌더링, 나머지(다이얼로그 다수, 설정 화면 등)는 QWidgets 유지 |
| C. 전면 QML 전환 | 앱 전체 UI를 QML/Qt Quick으로 재작성 |
| D. 완전히 다른 스택(Electron/Tauri/웹 프론트) | 별도 문서에서 이미 검토·기각(§7 참고) |

이 문서는 A vs B vs C를 중심으로, D는 이미 내려진 결론을 재확인하는 수준으로만 다룬다.

## 2. QML이 실제로 주는 것 (기술적 사실관계)

- **같은 Qt 생태계다.** QML(Qt Quick)은 별도 회사/벤더 프레임워크가 아니라 Qt 자체가 "선언적·애니메이션 친화적 UI"를 위해 만든 계층이다. PySide6는 `QtQml`/`QtQuick`/`QtQuickWidgets` 바인딩을 공식 지원한다.
- **애니메이션이 1급 시민이다.** `Behavior`, `Transition`, `NumberAnimation`, `states` 등이 언어 차원에서 지원돼, "값이 바뀌면 자동으로 부드럽게 보간"같은 걸 QWidgets보다 훨씬 적은 코드로 표현한다. GPU 가속 씬그래프 렌더링이라 복잡한 애니메이션도 QWidgets보다 매끄럽다.
- **부분 도입이 기술적으로 가능하다.** `QQuickWidget`으로 기존 QWidgets 창 안에 QML 화면 하나를 끼워 넣을 수 있어, 전면 재작성 없이 특정 화면(예: 대시보드)만 시범 도입하는 게 가능하다.
- **비즈니스 로직은 안 바뀐다.** 스캔 엔진(`core/`)·DB 레이어(`utils/db_connector.py`)·보안 레이어는 QML 도입 여부와 무관 — 영향 범위는 순수하게 프레젠테이션 레이어(`gui/`)에 국한된다.

여기까지는 QML이 기술적으로 "더 나은 UI/UX"를 만들 수 있는 도구라는 사실관계이고, 그 자체로는 채택 여부의 답이 아니다. 아래부터가 실제 판단 근거다.

## 3. 비용(Cost) — 구체적으로 무엇이 드는가

### 3.1 학습 곡선
QML은 Python이 아니라 별도의 선언적 DSL(JS와 유사한 문법)이다. 지금 이 팀은 Python 단일 언어로 스캔 엔진부터 GUI까지 전부 짜고 있는데, QML을 들이면 "언어가 두 개인 코드베이스"가 된다. 상태 바인딩·시그널 연결 방식도 QWidgets와 패턴이 달라 재학습이 필요하다.

### 3.2 마이그레이션 비용 — 타이밍이 특히 안 좋다
**바로 이번 세션에서 QWidgets 기반으로 "신뢰할 수 있는 작업대" 리디자인(색상 토큰, 탭 역할 분리)을 막 끝냈다.** 지금 QML로 옮기면 그 작업을 다른 기술로 다시 해야 한다 — 매몰비용 오류로 "이미 투자했으니 계속 QWidgets를 써야 한다"는 뜻이 아니라, **정반대로: 방금 끝낸 작업이 QWidgets에 남아있는 채로 재사용 가능한 지금이, 그걸 버리고 QML로 다시 그리는 것보다 명백히 저렴한 시점**이라는 뜻이다.

### 3.3 빌드 파이프라인 리스크
현재 빌드(`ci/build_final_v3.ps1`)는 Cython 컴파일 + PyArmor 난독화 + PyInstaller `--onefile`의 하이브리드 구조고, 이미 알려진 취약점이 있다:
- PyArmor 무료 라이선스가 `main_window.py`를 "too big to obfuscate"로 거부해 예외 처리 중인 상태(§ 기술명세서 5절).
- MSVC/SDK 경로 이슈 등 환경 의존성 문제가 세션 메모리에 기록될 정도로 이미 까다로움.

QML을 추가하면 `--collect-all`/`--collect-qmlfiles`류의 새 PyInstaller 옵션과 QML 모듈 경로 번들링이 필요해지는데, **PyInstaller+QML 조합은 커뮤니티에서도 종종 "빌드는 되는데 실행하면 QML 모듈을 못 찾는다" 류의 문제가 보고되는 조합**이다. 이미 불안정한 빌드 파이프라인에 새로운 실패 지점을 추가하는 셈이다. `--onefile`도 QML의 수많은 소규모 리소스 파일과 궁합이 안 좋아 `--onedir` 전환까지 같이 검토해야 할 수 있다(이건 이미 별도 로드맵으로 미뤄둔 항목).

### 3.4 팀 구성 리스크
이 세션 초반 논의에서 확인됐듯 사실상 1인 개발 체제다(하드웨어 보안키/PC 3대 논의 참고). QML을 새로 배우고 적용하는 학습·디버깅 부담이 전부 한 사람에게 집중된다 — 프론트엔드 전담 인력이 없는 상태에서 새 UI 패러다임을 도입하는 리스크는 팀 규모가 클 때보다 훨씬 크다.

### 3.5 비용 요약 (정성적 추정)

| 항목 | A(현행+애니메이션) | B(부분 QML) | C(전면 QML) |
|---|---|---|---|
| 학습 곡선 | 거의 없음(기존 Qt API 확장) | 중간 | 큼 |
| 재작업 범위 | 없음 | 대시보드 1개 화면 | GUI 파일 14개 전부 |
| 빌드 파이프라인 리스크 | 없음 | 낮음~중간(부분 실패 시 QWidgets로 롤백 가능) | 큼 |
| 방금 끝낸 리디자인 재사용 | 100% | 대시보드 외 100% | 0% |

## 4. 효익(Benefit) — 이게 정말 상품 가치를 높이는가

여기가 핵심 질문이다. **"인터렉티브하고 애니메이션 있는 UI"가 이 제품의 실제 구매 결정 요인인가?**

이 세션 전체에서 이미 정리된 이 제품의 실질 차별점을 다시 보면:
- KISA 룰셋 커버리지(230+개 라이브)와 판정 정확도(`vulnerable_keyword` 전수검토 등)
- hostname 4단 폴백 + 커버리지 추적(감사 완결성)
- OT/저속 모드(안전 우선)
- 룰셋 증분 업데이트(서명 검증)
- **타겟 세그먼트 구매 논리**: 대기업 = "가격 아닌 확실성", 중소 = "감당 가능한 가격의 유일한 대안"

이 목록 어디에도 "화려한 애니메이션"이 구매 결정 요인으로 등장하지 않는다. 오히려:
- **대기업 세그먼트("확실성")**: 판정 정확도·완결성·감사 근거자료 품질이 확실성의 근거이지, UI 애니메이션이 "확실함"을 증명하지 않는다.
- **중소 세그먼트("유일한 대안")**: 가격 대비 기능이 구매 이유지, UI 화려함이 아니다.

### 4.1 그래도 QML이 진짜 가치를 낼 수 있는 시나리오
- **영업 데모/피칭 상황.** 이사님이 대기업 담당자 앞에서 라이브 데모나 영상을 보여줄 때, 화면이 매끄럽게 반응하는 인상은 "이 회사가 완성도 있는 제품을 만든다"는 신뢰 신호로 작용할 수 있다. 이건 실질 기능이 아니라 **첫인상/설득력 문제**이므로, 다른 차별점들과는 결이 다르지만 무시할 근거는 아니다.
- **경쟁 제품과의 실제 비교 시연.** 퀄리스/클레로티 UI가 이미 세련된 SaaS급이면, 나란히 비교됐을 때 격차가 눈에 띌 수 있다.

다만 이 두 시나리오는 **"영업 과정에서 실제로 UI 완성도가 거래를 갈랐다"는 피드백이 확인된 뒤에 투자하는 게 순서**이지, 지금 선제적으로 큰 비용을 들일 근거로는 약하다.

## 5. Option A만으로 얻을 수 있는 것 (저비용 대안의 실제 효과)

QML 없이 `QPropertyAnimation`/`QGraphicsOpacityEffect`만으로도 상당 부분 커버된다:
- KPI 카드 숫자 카운트업
- 경고 배너 페이드인/아웃
- 카드 호버 반응
- 커버리지 게이지 애니메이션 채움
- 탭 전환 시 살짝 페이드/슬라이드

이 정도만 해도 "정적인 표 화면"에서 "반응하는 화면"으로 체감이 크게 바뀐다. **QML 대비 비용은 거의 0에 가깝고(이미 있는 라이브러리, 기존 코드베이스에 함수 몇 개 추가), 효과의 상당 부분(어쩌면 80% 이상)을 가져간다.**

## 6. 권고안

1. **지금은 Option A로 간다.** 애니메이션 아이디어(§ 이전 대화에서 제안한 5가지)를 QWidgets 안에서 구현.
2. **QML(Option B)은 다음 조건 중 하나가 실제로 확인되면 재검토한다:**
   - 영업 과정에서 "UI가 경쟁 제품보다 뒤떨어져 보인다"는 피드백이 실제로 나옴
   - 대기업 대상 데모/PT 자료에 임베드할 인터렉티브 데모가 별도로 필요해짐
   - 프론트엔드를 전담할 인력이 팀에 합류함(1인 개발 리스크 해소)
3. **Option C(전면 전환)는 이 시점에 근거가 없다.** 굳이 시도한다면 지금까지 쌓은 기능 로드맵(Oracle 서비스명 UI, 룰셋 업데이트, 커버리지 추적 등)이 전부 뒤로 밀리는 기회비용이 발생하는데, 그걸 정당화할 만한 수요 신호가 아직 없다.
4. Option B를 나중에 하게 되더라도 **대시보드 1개 화면부터 `QQuickWidget`으로 격리 도입**하는 걸 권한다 — 실패해도 롤백 범위가 작고, 나머지 13개 GUI 파일은 그대로 QWidgets로 남아 리스크가 전파되지 않는다.

## 7. 참고 — 완전히 다른 스택(Electron 등) 재확인

이전 논의에서 이미 기각된 내용을 짧게 재확인만 한다: Electron/웹 기반은 Chromium+Node 번들로 인해 이 제품의 핵심 포지셔닝("가볍고 OT 친화적")과 정면 충돌하고, Python 백엔드-JS 프론트 IPC 브릿지라는 불필요한 아키텍처 복잡도가 추가된다. QML과 달리 이건 진짜 "다른 프레임워크"라 이 문서의 비교 대상에서 제외했다.
