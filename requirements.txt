# Z-VulnScan Professional Edition - 런타임 의존성
#
# 이 파일이 런타임 의존성의 단일 소스다. `pip install -r requirements.txt`로
# 개발 환경을 구성하고, CI(.github/workflows/release.yml)도, `library.py`(로컬
# 원클릭 설치 스크립트)도 전부 이 파일을 그대로 쓴다 - library.py가 예전엔 별도
# 하드코딩 목록(PyQt5/scapy 등 실제 코드와 이미 어긋나 있었음)을 따로 들고 있었으나
# 2026-08-05 정리하며 이 파일을 그대로 설치하도록 바꿨다.
#
# 빌드 전용 도구(Cython/PyArmor/PyInstaller)는 여기 포함하지 않는다 - 최종
# 사용자에게 배포되는 exe 실행에는 필요 없고, 빌드하는 사람/CI만 필요하기 때문.
# (워크플로우에서 별도로 pip install cython pyarmor pyinstaller 실행)

PySide6
paramiko
pywinrm
openpyxl
reportlab
keyring
cryptography
requests

# core/database_inspector.py가 엔진별로 조건부 import한다(pymysql/psycopg2는
# 이전부터 쓰였으나 이 파일엔 등록이 안 돼 있었음 - builing.ps1의 --collect-all로만
# 빌드에 포함되고 있었음. pymssql/oracledb는 MSSQL/Oracle(D-xx) 지원 추가로 새로
# 필요해짐 - oracledb는 thin 모드로 Oracle Instant Client 없이 순수 Python 접속).
pymysql
psycopg2-binary
pymssql
oracledb

# paramiko가 특정 키 타입(Ed25519 등) 처리에 선택적으로 쓰는 의존성 -
# 명시적으로 import하진 않지만 build_final_v3.ps1이 --collect-all로 묶는다.
# utils/rule_update.py의 Ed25519 서명 검증도 cryptography(위)로 이미 커버된다.
bcrypt
pynacl
